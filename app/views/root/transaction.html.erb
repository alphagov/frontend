<div id="wrapper" class="transaction">

	<!-- naughty related position hack -->
	<div class="related-positioning naughty">
		<div class="related-container naughty">
			<div class="related-container-two naughty">
	   		<div class="related">
			     <div class="inner" class="group">
			       <h2>Related tools &amp; topics</h2>
			       <nav role="navigation">
			         <ul>
                 <%= (@transaction.related_items || "").html_safe %>
								 <li class="related-topic"><a href="#">More in <span><%= @transaction.section %></span>&hellip;<span class="arrow"></span></a></li>
			         </ul>
			       </nav>
			     </div>
			   </div><!-- end .related -->
			</div>
		</div>
	</div>
	<!-- end naughty related position hack -->

  <section role="main" class="group">

    <header class="group">
      <div class="inner">

        <hgroup>
          <h1><span>Services</span> <%= @transaction.title %></h1>
        </hgroup>

      </div>
    </header>

    <article role="article" class="group">
      <div class="inner">

        <div class="intro">
          <%= Govspeak::Document.new(@transaction.introduction || "").to_html.html_safe %>

          <p class="get-started"><a href="<%= @transaction.link %>" title="Begin <%= @transaction.title %> on the <%= @transaction.will_continue_on %> site in a new browser window" rel="external" class="button">Get started &rarr;</a></p>

          <% if @transaction.will_continue_on != '' %>
            <p>This will continue on <%= @transaction.will_continue_on %>.</p>
          <% end %>

        </div>

        <h2>What you need to know</h2>

        <div class="more">
          <ul class="helpers">
          <% @transaction.expectations.each do |e| %>
            <li class="<%= e.css_class %>">
              <%= e.text %>
            </li>
          <% end %>
          </ul>

          <%= Govspeak::Document.new(@transaction.more_information || "").to_html.html_safe %>
        </div>

      </div>

  		<div class="meta-data">
  			<p class="modified-date">Last updated: <%= @transaction.updated_at.strftime("%d %B %Y") %></p>
  			<p>Re-use this data:</p>
				<ul class="api-links">
          <li class="json"><a href="/publications/<%= @transaction.slug %>.json">JSON</a></li>
        </ul>
  		</div>

    </article>

  </section>

</div>
