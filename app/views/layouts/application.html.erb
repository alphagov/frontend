<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>
<% if @publication %>
      <%= @publication.alternative_title.nil? ? @publication.alternative_title : @publication.title %> |
      <% unless @publication.section.nil? or @publication.section == ''%>
      <%= @publication.section %> |
<% end %>
      Beta.gov.uk
<% else %>
      <%= yield :title %>
<% end %>
  </title>
  <%= stylesheet_link_tag '/stylesheets/publisher.css' %>
<% if @publication %>
  <% if @publication.overview and @publication.overview != '' %>
    <meta name="description" content="<%= @publication.overview %>">
  <% end %>
  <% if @publication.section and @publication.section != '' %>
    <meta name="x-section-name" content="<%= @publication.section %>">
    <meta name="x-section-link" content="/<%= @publication.section.parameterize %>">
  <% end %>
<% else %>
  <%= yield :description %>
<% end %>
  <%= javascript_include_tag '/javascripts/jquery.mustache.js',"/javascripts/geo-locator.js"  %>
  <%= yield :extra_javascript %>
</head>
<body>
<% if @publication %>
  <div id="wrapper" class="<%= @publication.type %>">
<% else %>
  <div id="wrapper" class="<%= yield :wrapper_class %>">
<% end %>
	<%= render :partial => 'location_header' %>  
<%= yield %>
 </div>
</body>
</html>
