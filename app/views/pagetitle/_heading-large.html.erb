<%= render "govuk_publishing_components/components/heading", {
  text: "Another heading with a number of words in it so large that it spreads onto multiple lines and causes all kinds of potential vertical spacing issues that are turning out to be a real headache",
  font_size: "xl",
  heading_level: 1,
  margin_bottom: 8,
} %>
