<%
  inverse = options[:inverse] || false
  margin_bottom = options[:margin_bottom] || 0
%>
<%= render "govuk_publishing_components/components/govspeak", {
  inverse: inverse,
  margin_bottom:,
} do %>
  <%= block.data["content"]["content"].html_safe %>
<% end %>
