<% if flash[:validation]&.reject(&:empty?)&.any? %>
  <%= render "govuk_publishing_components/components/error_summary", {
    title: t("funding_form.errors.heading"),
    items: flash[:validation],
  } %>
<% end %>
