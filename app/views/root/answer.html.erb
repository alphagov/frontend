<section id="content" role="main" class="group">

  <header class="page-header group">
      <hgroup>
        <h1><span>Quick answer</span> <%= @answer.title %></h1>
      </hgroup>
  </header>
	<div class="article-container group">
	  	<article role="article" class="group">
	     <div class="inner">
	       <%= to_govspeak(@answer.body) %>
	     </div>
	   </article>
    <%= render 'publication_metadata', :publication => @answer, :artefact => @artefact, :api_links => { 'application/json' => publication_path(@answer.slug, :edition => @edition, :format => :json) } %>
	
    <div id="report-a-problem"></div>	
	</div>

</section>

<div id="related-items"></div>

