
	<!-- related position hack -->
	<div class="related-positioning">
		<div class="related-container">
			<div class="related-container-two">
	   		<div class="related">
			     <div class="inner" class="group">
			       <h2>Related tools &amp; topics</h2>
			       <nav role="navigation">
			         <ul>
                 <%= (@transaction.related_items || "").html_safe %>
								 <li class="related-topic"><a href="">View more in <span><%= @transaction.section %>&rarr;</span></a></li>
			         </ul>
			       </nav>
			     </div>
			   </div><!-- end .related -->
			</div>
		</div>
	</div>
	<!-- end related position hack -->

  <section role="main" class="group">

    <header class="group">
      <div class="inner">

        <hgroup>
          <h1><span>Services</span> <%= @transaction.title %></h1>
        </hgroup>

      </div>
    </header>

    <article role="article" class="group">
      <div class="inner">

        <div class="intro">
          <%= to_govspeak(@transaction.introduction) %>

          <p class="get-started"><a href="<%= @transaction.link %>" title="Begin <%= @transaction.title %> on the <%= @transaction.will_continue_on %> site in a new browser window" rel="external" class="button">Get started &rarr;</a></p>

          <% if @transaction.will_continue_on != '' %>
            <p>This will continue on <%= @transaction.will_continue_on %>.</p>
          <% end %>

        </div>

        <h2>What you need to know</h2>

        <div class="more">
          <ul class="helpers group">
          <% @transaction.expectations.each do |e| %>
            <li class="<%= e.css_class %>">
              <%= e.text %>
            </li>
          <% end %>
          </ul>

          <%= to_govspeak(@transaction.more_information) %>
        </div>

      </div>

  		<div class="meta-data">
  			<p class="modified-date">Last updated: <%= @transaction.updated_at.to_formatted_s(:short_ordinal) %></p>
  			<p>Re-use this data:</p>
				<ul class="api-links">
          <li class="json"><a href="<%= publication_path(@publication.slug,:edition=>@edition,:format=>:json) %>">JSON</a></li>
        </ul>
  		</div>

    </article>

  </section>
