<% if person.currently_in_a_role? %>
  <div id="current-roles">
    <% person.current_roles.each do |role| %>
      <div class="role_appointment role govuk-!-padding-bottom-8">
        <%= render "govuk_publishing_components/components/heading", {
          text: role.title,
          margin_bottom: 2,
        } %>

        <%= render "govuk_publishing_components/components/govspeak", {
          direction: page_text_direction,
        } do %>
          <%= role.body&.html_safe %>
        <% end %>

        <% if role.document_type == "ministerial_role" %>
          <p class="govuk-body-s">
            <%= link_to t("formats.roles.read_more"), role.base_path, class: "govuk-link" %>
          </p>
        <% end %>

        <% if role.parent_organisations.any? %>
          <p class="govuk-body">
            <%= safe_join(
              role.parent_organisations.map { |organisation| link_to(organisation.title, organisation.base_path, class: "govuk-link") },
              " #{t('common.and')} ",
            ) %>
          </p>
        <% end %>
      </div>
    <% end %>
  </div>
<% end %>
