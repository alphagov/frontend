<%= render "govuk_publishing_components/components/warning_text", {
  text: sanitize("To obtain this licence, you need to contact the authority directly. To continue, go to #{link_to(licence_details.authority["name"], licence_details.authority.dig("actions", locals[:action])[locals[:index]]["url"], class: "govuk-link")}.")
} %>
