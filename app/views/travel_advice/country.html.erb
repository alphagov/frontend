<section id="content" role="main" class="group multi-page">

  <header class="page-header group">
    <hgroup>
      <h1><span>Foreign travel advice </span><%= @publication.country['name'] %></h1>
    </hgroup>
    <nav class="skip-to-related">
      <a href="#related"><%= t 'common.not_what_youre_looking_for' %> ↓</a>
    </nav>
  </header>

  <div class="article-container group">
    <%= render :partial => "travel_advice_navigation" if @publication.parts.present? %>

    <article role="article" class="group">
      <div class="inner">

        <% if @publication.current_part %>
          <header>
            <h1><%= @publication.current_part.title %></h1>
          </header>

          <%= raw @publication.current_part.body %>
        <% else %><%# The summary page %>
          <%= render :partial => "country_summary", :locals => {:publication => @publication} %>
        <% end %>
      </div>
    </article>

    <div id="report-a-problem"></div>
  </div>

</section>

<div id="related-items"></div>

<% content_for :extra_headers do %>
  <link rel="alternate" type="application/json" href="<%= publication_api_path(@publication, :edition => @edition) %>" />
  <%= auto_discovery_link_tag :atom, travel_advice_country_path(@publication.country['slug'], :format => :atom),
    :title => "Recent updates for #{@publication.country['name']}" %>
  <%- if @publication.current_part and @publication.has_previous_part?(@publication.current_part) -%>
    <link rel="prev" href="<%= part_path(@publication.slug, @publication.part_before(@publication.current_part).slug,@edition) %>" />
  <%- end -%>
  <%- if @publication.current_part and @publication.has_next_part?(@publication.current_part) -%>
    <link rel="next" href="<%= part_path(@publication.slug, @publication.part_after(@publication.current_part).slug,@edition) %>" />
  <%- end -%>
<% end %>
